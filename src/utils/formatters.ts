export const formatPrice = (price: number): string => {
  return `£${price.toFixed(2)}`;
};

export const formatPhoneNumber = (phone: string): string => {
  // UK phone number formatting helper
  return phone;
};
